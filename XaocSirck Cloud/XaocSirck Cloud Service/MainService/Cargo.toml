[package]
name = "main-service"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../Common" }
cache-service = { path = "../CacheService" }
axum = "0.7"
tokio = { version = "1", features = ["full"] }
